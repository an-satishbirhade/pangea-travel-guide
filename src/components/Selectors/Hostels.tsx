import * as React from 'react';

interface IHostelsProps {
}

const Hostels: React.FunctionComponent<IHostelsProps> = (props) => {
  return (
    <h1>Hostels Works</h1>
  )
};

export default Hostels;
