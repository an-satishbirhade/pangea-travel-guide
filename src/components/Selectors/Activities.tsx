import * as React from 'react';

interface IActivitiesProps {
}

const Activities: React.FunctionComponent<IActivitiesProps> = (props) => {
  return (
    <h1>Activities Works</h1>
  )
};

export default Activities;
