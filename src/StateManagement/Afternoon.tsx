import * as React from 'react';

interface IAfternoonProps {
}

const Afternoon: React.FunctionComponent<IAfternoonProps> = (props) => {
  return (
    <h1>Afternoon Component</h1>
  )
};

export default Afternoon;
