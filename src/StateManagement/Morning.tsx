import * as React from 'react';

interface IMorningProps {
}

const Morning: React.FunctionComponent<IMorningProps> = (props) => {
  return (
    <h1>morning component</h1>
  )
};

export default Morning;
