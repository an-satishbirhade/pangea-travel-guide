import { IonButton, IonInput } from '@ionic/react';
import * as React from 'react';

interface IUseRefProps {
}

const UseRef: React.FunctionComponent<IUseRefProps> = (props) => {

  return (
    <>
      <h1>Hello</h1>
    </>
  );
}
export default UseRef;
